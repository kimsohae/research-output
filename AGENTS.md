# AGENTS.md

이 저장소는 리서치 결과를 채널별로 사람이 이해하기 쉬운 산출물로 변환하기 위한 스킬 모음이다.

## 목표

에이전트는 리서치와 글쓰기를 한 단계에서 처리하지 않는다.

기본 순서:

```text
Research
→ research-output
→ report-router
→ renderer
→ optional external style layer
→ report-lint
→ final
```

## 절대 규칙

### 1. research-output이 사실의 기준이다

최종 결과의 모든 중요한 사실은 `research-output`까지 추적 가능해야 한다.

renderer나 스타일 단계에서 새로운 사실을 추가하지 않는다.

### 2. 사실과 해석을 구분한다

다음을 서로 바꾸지 않는다.

- 확인된 사실
- 해석
- 추정
- 가설
- 권고

특히 가능성을 인과관계로 강화하지 않는다.

### 3. renderer는 표현만 바꾼다

허용:

- 순서 변경
- 압축
- 중복 제거
- 제목 변경
- 독자에 맞는 설명 추가
- 이미 존재하는 정보의 강조

금지:

- 새로운 수치
- 새로운 조사 결과
- 새로운 인과관계
- 근거 없는 확신 강화
- 원본에 없는 담당자, 일정, 결정 생성

### 4. Slack과 Confluence는 서로 다른 산출물이다

Confluence:
- 장기 보존
- 맥락 복원
- 근거 추적
- 비동기 이해

Slack:
- 즉시 파악
- 핵심 결론
- 필요한 행동
- 상세 문서 진입점

Slack은 Confluence 문서를 단순히 잘라 만든 결과가 아니다.

### 5. 스타일 단계는 선택 사항이다

외부 한국어 스타일 스킬을 사용할 수 있지만 이 저장소에는 포함하지 않는다.

스타일 단계 이후 반드시 `report-lint`를 실행한다.

### 6. 최종 lint 실패 시 수정한다

Critical 이슈가 있으면 사용자에게 출력하지 않는다.

Major 이슈도 가능한 한 수정한 뒤 출력한다.

## 어떤 스킬을 사용할지

- 리서치 결과를 정리해야 한다 → `research-output`
- 어떤 채널로 만들지 결정해야 한다 → `report-router`
- Confluence 게시용이다 → `confluence-report`
- Slack 공유용이다 → `slack-report`
- 최종 검증이 필요하다 → `report-lint`

## 좋은 결과의 기준

좋은 결과는 문장이 예쁜 결과가 아니다.

독자가 다음을 빠르게 이해할 수 있어야 한다.

- 무엇을 알게 되었는가
- 무엇이 근거인가
- 무엇이 아직 불확실한가
- 그래서 무엇을 의미하는가
- 무엇을 해야 하는가

## 예시 사용

`examples.md`의 문장을 복사하지 않는다.

예시는 다음을 학습하기 위한 것이다.

- 정보 순서
- 압축 수준
- 길이
- 채널별 목적
- 사실과 해석의 경계
- 행동 요청 방식

새로운 입력과 예시가 충돌하면 항상 새로운 입력의 `research-output`을 우선한다.
